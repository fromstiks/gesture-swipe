<resources>

    <!-- Soft raised card that blends with the background. -->
    <style name="NeuCard" parent="Widget.Material3.CardView.Elevated">
        <item name="cardBackgroundColor">@color/surface</item>
        <item name="cardCornerRadius">18dp</item>
        <item name="cardElevation">4dp</item>
        <item name="contentPadding">14dp</item>
    </style>

    <!-- Main soft 3D button. -->
    <style name="NeuButton" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/btn_fill</item>
        <item name="android:textColor">@color/btn_text</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">13sp</item>
        <item name="android:minHeight">42dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="cornerRadius">16dp</item>
        <item name="android:elevation">4dp</item>
        <item name="android:insetTop">2dp</item>
        <item name="android:insetBottom">2dp</item>
        <item name="rippleColor">#33000000</item>
    </style>

    <!-- Smaller button for the one-time setup accordion. -->
    <style name="NeuButton.Small" parent="NeuButton">
        <item name="android:textSize">12sp</item>
        <item name="android:minHeight">38dp</item>
    </style>

    <!-- Primary accent button (Start / Stop). -->
    <style name="NeuButton.Primary" parent="NeuButton">
        <item name="backgroundTint">@color/primary_fill</item>
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:minHeight">50dp</item>
        <item name="android:textSize">15sp</item>
    </style>

    <!-- Accordion header (tap to expand). -->
    <style name="AccordionHeader">
        <item name="android:textColor">@color/on_surface</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">12dp</item>
        <item name="android:background">?android:attr/selectableItemBackground</item>
    </style>

    <!-- Small grey helper/comment text. -->
    <style name="HelpText">
        <item name="android:textColor">@color/on_surface_var</item>
        <item name="android:textSize">11sp</item>
        <item name="android:paddingStart">4dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:lineSpacingExtra">2dp</item>
    </style>

</resources>
